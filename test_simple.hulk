
type Point(x,y) {
   x = x;
   y = y;
   getX() => self.x;
   getY() => self.y;
   setX(x) => self.x := x;
   setY(y) => self.y := y;
};






3+4-5;
3 + 4 * 2;
(2 + 3) * 4;
10 % 3 + 2^3;
3 < 4 && 2 > 1;
a == b || x != y;
1 + 2 * 3 > 5 && 4 < 10;
"a" @ "b";
x @@ y;
"a" @ "b" + "c";




if (x > 5) { y = 10; } else { y = 0; };
while (i < 10) { i = i + 1; };
for (x in [1,2,3]) 
{ print(x); };
{ 
    a = 1;
    b = 2;
};
while (a > 0) a := a - 1;
while (a > 0) { print(a); }
for (x in range(0, 10)) print(x);